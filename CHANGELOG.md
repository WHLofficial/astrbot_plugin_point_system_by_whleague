# Changelog

## v0.5.5 (2026-08-15)

### 修复

- **打劫被"目标被劫次数上限"拦截时不再消耗打劫者冷却**：此前打劫者发起打劫会先写入冷却、后检查目标上限，被拦截后仍进入冷却（默认 10 分钟），无法立即转打其他目标；现在该拦截会清除本次冷却，打劫者可立即转打其他目标。打劫者自身每日上限、失败扣成本不足等其他拦截仍消耗冷却（现状不变）

### 变更

- **兑换列表优化**：横线分隔符由 2 条减为 1 条；兑换物品超过 3 条时整份列表改为伪造聊天记录（合并转发卡片）发送——昵称「兑换小铺」、由 bot 发出，群里仅见一条转发卡片，展开才见完整列表（≤3 条仍为纯文本）

### 测试

- 反转目标被劫上限拦截的冷却断言：拦截后可立即转打其他目标、冷却剩余为 0（s19）
- 新增范围锁定用例：打劫者自身每日上限拦截仍消耗冷却（s19）
- 兑换列表断言：≤3 物品纯文本仅含 1 条横线；>3 条走转发卡片（昵称「兑换小铺」、bot 自身、含全部物品）（s10）

## v0.5.4 (2026-08-12)

### 变更

- 兑换指令 `/兑换商品` 更名为 `/商品兑换`，注册别名 `/兑换`（旧指令 `/兑换商品` 不再生效）；指令图 usage 示例与注册指令名严格对齐，修复历史遗留的示例 `/兑换` 与指令名不符问题
- 兑换列表展示优化：物品 ID 醒目化（`【ID N】`），列表头部/底部新增兑换用法提示
- emoji 统一：对号统一为 ✅、叉号统一为 ❌（兑换记录状态图标 ✔ → ✅）

### 测试

- 路由用例同步新指令名并新增别名 `/兑换` 用例（s10）
- 新增别名匹配语义测试：别名可触发、旧指令失效、与 `/兑换记录` 前缀互斥（s17）

## v0.5.3 (2026-08-06)

### 新增

- **管理员查看兑换记录时展示兑换者**：`/兑换记录 all|pending` 列表行尾显示 `昵称(QQ)`（昵称解析失败时仅显示 QQ，如 `⏳ R20260101-0001 物品1x1 10积分 小红(123456789)`）；`/兑换记录 <record_no>` 详情新增「兑换者: 昵称(QQ)」行（成员查看本人记录同样可见，无越权面）
- 昵称实时解析（不落库）：优先群名片 card，其次昵称 nickname，控制字符经 `clean_display_name` 剥离（与排行榜/查生日一致）；**两级群回退**——先查兑换发生时的记录群，查不到再查管理员当前群，均失败回退 QQ；跨群全局管理员按各记录自身群解析；批量 `asyncio.gather` 并发，每页最多 10 条、API 调用开销与积分榜同级

### 测试

- 新增管理员列表/详情断言（s10）：无 bot 回退 QQ 行格式、本群群管仅本群记录、数字 QQ/群 + FakeBot 验证昵称路径（列表与详情）；跨群隔离列表行格式断言（s3）；全量 **197/197** 通过

## v0.5.2 (2026-08-05)

### 修复

- **主动发送 origin 首段改用机器人名称**（平台适配器实例 id，即事件会话 `platform_name`）：核销跨群/私信通知与生日 cron 播报此前用平台类型名（如 `aiocqhttp`）构造 `context.send_message` 的 session，与 `platform.meta().id` 匹配条件不符，导致消息静默发不出去。修复后核销通知优先取 `event.get_platform_id()`（回退 `unified_msg_origin` 首段）；生日报播遍历平台实例逐个尝试
- **生日报播仅在发送成功时才标记已播报**：此前忽略 `send_message` 返回值，平台未匹配时也照常标记，失败无法重试

### 测试

- `FakeEvent` 新增 `platform_id`/`get_platform_id`/`unified_msg_origin`（与生产对齐）；核销通知 origin 断言改为机器人名称，新增机器人名称缺失时回退 `unified_msg_origin` 首段用例
- 生日报播新增多实例发送失败 / 实例缺失 → 不标记已播报、可重试用例

## v0.5.1 (2026-08-05)

### 变更

- `/兑换` 更名为 `/兑换商品`，避免与其他插件指令潜在冲突（`/兑换记录`、`/添加兑换` 等独立指令名不变）
- 核销通知备注格式：通过 `已通过核销（备注：…）`、驳回 `已退回（理由：…）`，备注不再直接拼接在文案后（无备注时维持原兜底文案）

### 测试

- 目录完整性测试自动校验 `/兑换商品`，路由用例同步指令名；核销通知格式断言同步 `（备注：…）`/`（理由：…）`

## v0.5.0 (2026-08-05)

### 新增

- **核销跨群**：全局管理员（AstrBot 全局管理员或管理员名单中 `group_id` 为空的全局管理员）可在任意群核销/驳回其他群的兑换记录，`/兑换记录` 单条详情查看口径同步统一（群管理员仍仅限本群）
- **核销通知渠道配置** `redeem_notify_channel`（WebUI 下拉菜单选择）：
  - `group`（默认）= 原兑换群 @通知，同群走当前会话、跨群经主动发送
  - `private` = 私信通知兑换者（需平台支持）
  - 发送失败时在当前会话发警告兜底，状态结果不受通知成败影响
  - `/设置` 支持中英归一化：`group/群`、`private/私信/私聊`，非法值拒绝

### 测试

- 新增跨群核销权限矩阵（群管拒绝/全局管理员跨群/表级全局管理员）、group/private 双渠道发送 origin 断言、无 context 兜底警告、详情视图口径一致回归
- 配置枚举校验断言（归一化/非法值拒绝）；测试桩 `MessageType` 对齐生产值（`GroupMessage`/`FriendMessage`）

## v0.4.3 (2026-08-04)

### 新增

- **目标被劫上限动态方案** `rob_target_limit_dynamic`（默认 false，与原固定方案并列，可随时切换）：
  - 关闭（默认）：维持原行为，目标每日被劫上限 = 固定值 `rob_target_daily_limit`
  - 开启：目标每日被劫上限 = `rob_target_daily_limit`（固定基准值）+ 该人今日**主动发起打劫次数**（全部口径，成功与失败均计数，复用 `count_robs_today` 与 `idx_rob_qq_date` 索引）——参与打劫者暴露更多被劫窗口，被动挂机者维持基准保护
  - 0 语义：固定方案 `rob_target_daily_limit=0` 表示不限（与 v0.4.2 一致）；**动态方案基准最小 1**——`/设置` 拒绝 0（含「基准为 0 时开启动态方案」反向路径），WebUI/手改配置为 0 时 `_load_config_cache` 加载按 1 处理并回写持久化，服务层 `max(1, 基准)` 防御兜底
  - 拦截文案：动态方案附当前上限「目标今日已被打劫 X 次（今日上限 Y），无法再被打劫」；固定方案文案不变
  - 实现：`rob_service._tx` 事务内统计，与打劫者限次/目标被劫计数并列（`BEGIN IMMEDIATE` 串行化保证并发边界正确），无 DB 结构改动

### 测试

- 新增动态方案用例（s19/s9/s15）：上限 = 基准 + 主动次数精确边界（8 放行/第 9 拒、文案含「今日上限 8」）、主动打劫**失败**同样计数（失败 1 次后上限 7）、基准 0 服务层按 1 钳位（非不限）、`/设置` 拒绝 0 两方向（设置 0/开启动态）且固定方案 0 仍合法、加载时 0→1 强制并回写；全量回归通过

## v0.4.2 (2026-08-03)

### 新增

- **打劫防集火**（目标侧保护，修复高积分玩家被集火掠夺）：
  - 目标每日被劫上限 `rob_target_daily_limit`（默认 6，0=不限）：按 target_qq 全局跨群统计，**成功与失败均计数**；达上限后打劫被拒（"目标今日已被打劫 X 次，无法再被打劫"），拦截不写记录、打劫者当日次数不消耗但进入冷却（与打劫者自身超限语义一致）
  - 打劫收益衰减 `rob_reward_decay`（默认 0.25，0~1，0=不衰减）：目标每被**成功**打劫一次，后续收益 ×(1-decay)^n（失败不计入衰减）；目标每日上限关闭时衰减仍生效，长期集火收益指数递减
  - 统计实现：`dao.target_robs_today(conn, target_qq)` 一次查询返回 (总次数, 成功次数)（`COALESCE(SUM(success),0)` 防 NULL），复用 `idx_rob_target` 索引，事务内与打劫者限次并列（`BEGIN IMMEDIATE` 串行化保证并发边界正确）

### 测试

- 新增 6 项防集火用例（s19）：目标限次（6 人集火第 7 拒/精确次数文案/拦截消耗冷却/不写记录）、0=不限放行且衰减仍生效（200/150/112/84/63/47/36）、衰减序列（cap 触顶 200/150/112/84/63）、失败不衰减（失败后成功仍全额 100）、decay=0 恒 200、decay=1.0 归零走防御路径不报错；全量 **191/191** 通过

## v0.4.1 (2026-08-03)

### 修复

- 打劫「打劫@群友」无任何响应：`parse_rob_message` 组件类型判定用 `str(c.type).lower()` 与 `"at"/"plain"` 比较，而真实平台组件 `type` 为 str 枚举（`str(ComponentType.Plain)` 返回全名 `"ComponentType.Plain"` 而非成员值 `"Plain"`），二者永不相等 → 目标/文本永不被识别，handler 静默不产出；改为 `==` 直接比较（str 枚举成员与字符串相等），兼容真实枚举、测试桩字符串与大小写

### 测试

- 新增回归用例 `matcher_real_enum`（s19）：以 `str, Enum` 模拟真实组件 type（修复前失败、修复后通过），并清理函数内 import 排序；全量 **185/185** 通过

## v0.4.0 (2026-08-03)

### 新增

- **打劫玩法**（无前缀「打劫 @目标」触发，仅群聊）：@ 目标解析（排除 @全体成员与 bot 自身，多 @/@bot/@all/打劫自己均有明确提示），其余文本压缩空白后严格匹配 `keyword_rob`（默认「打劫」）；打劫者积分 ≥ `rob_min_points`(100) 且非负分、目标积分 ≥ `rob_target_min_points`(50) 且非负分才可发起
- **收益公式**：`stolen = min(rob_reward_cap(200), rob_reward_fixed(50) + round(rob_reward_fixed × (目标积分/rob_reward_base_points(2000))^rob_reward_power(1.2)))`——目标 2000 分时抢 100，高分段凸曲线增长、200 分封顶；成功纯收益不扣成本，失败扣 `rob_cost`(50) 且不退还
- **防刷**：同用户冷却 `rob_cooldown`(600s，成功/失败均进入，冷却拦截显示实时剩余分钟，成功/失败反馈显示静态冷却行)；每日上限 `rob_daily_limit`(3 次，按 QQ 全局统计，0=不限，与抽奖口径一致)
- **负分联动**：打劫后对打劫者与目标均执行 `ensure_negative_title`（失败扣成本可能致负、目标可能被抢成负），联动「群女仆X号」头衔
- **数据**：新增 `rob_records` 打劫记录表（schema v4 → v5，纯新增表幂等建表，无需 ALTER）；流水新增 reason `rob_cost`（仅失败，不计累计获得）/ `rob_reward` / `rob_lost`（目标扣分，不计累计获得）
- **配置**：新增 12 项（`rob_enabled` / `rob_cost` / `rob_success_rate` / `rob_reward_fixed` / `rob_reward_base_points` / `rob_reward_power` / `rob_reward_cap` / `rob_min_points` / `rob_target_min_points` / `rob_cooldown` / `rob_daily_limit` / `keyword_rob`）；`rob_success_rate` 走 0~1 概率校验，`rob_reward_power` 单独 0~2 幂指数校验；`keyword_rob` 加入非空关键词校验（`_KEYWORD_KEYS`）
- **互斥闭环**：口令保留字集合加入 `keyword_rob`（防止口令=「打劫」双收益刷分）；活跃奖励扫描跳过打劫形态消息；指令图新增打劫条目（`{rob_kw}` 占位动态跟随配置）
- `utils/rate_limiter` 新增 `get_remaining`（冷却剩余秒数，cooldown≤0 或未冷却返回 0）；`utils/keyword_matcher` 新增 `parse_rob_message` / `is_rob_message`（组件属性 duck-typing 解析）

### 修复

- 打劫零收益极端配置（`rob_reward_cap`/`rob_reward_fixed` 为 0）下 `stolen=0` 触发 `change_balance(amount=0)` 报错：收益为 0 时跳过改分仅写记录，流程正常完成
- 门槛检查后目标被并发扣成负分时事务内幂运算崩溃：目标余额取 `max(0, ...)` 防御，按 0 计算收益

### 测试

- 新增打劫系统套件（s19，19 项）：收益公式锚点/round/封顶、失败扣成本不退还与记录、门槛矩阵（开关/群聊/自己/打劫者与目标门槛）、每日限次按 QQ 全局、成功/失败均进冷却、目标被抢成负分头衔联动（FakeBot 断言 set_group_card）、守卫失败整事务回滚、零收益与并发负值防御、匹配器形态判定、handler @ 解析（无/多/@bot/@all/自己/含字）、成功/失败反馈文案（含冷却行、无收益明细、昵称清洗）、活跃奖励跳过、口令保留字、指令图条目、事务回调 conn 方法不触发死锁保护
- 新增 `rate_limiter.get_remaining` 测试（s13）；迁移测试扩展 v1→v5 全链路（rob_records 表与索引存在，s15）
- 测试设施：`tests/stubs.py` 新增 `astrbot.api.message_components` 桩（Plain/At/AtAll，属性对齐真实组件）；`FakeEvent` 新增 `get_messages()` / `get_self_id()`（At/AtAll/@bot 组件链构造）
- 全量 **184/184** 通过

## v0.3.2 (2026-08-03)

### 新增

- 新增配置项 `backup_keep_count`（默认 30）：每个备份目录最多保留的备份文件数，超出自动删除最旧备份（仅清理本插件 `points_system_*.db` 命名文件，不误删其他文件），0 表示不清理；同时作用于每日备份与清空前备份目录
- 整数配置项业务上限校验（`/设置` 与 WebUI 共用入口 `validate_and_cast`）：超上限直接拒绝，共 22 个键（如 `lottery_cost ≤ 1,000,000`、`lottery_daily_limit ≤ 10,000`、冷却类 ≤ 86400、`cmd_map_cache_ttl_hours ≤ 87,600`、`backup_keep_count ≤ 10,000` 等）

### 修复

- `PointService.change_balance` 守卫分支（抽奖/兑换扣费）静默丢弃累计获得：现守卫路径显式传入 `earned_amount` 时同事务补记 `total_earned`（现有调用方均传 0/None，行为不变；消除未来调用方遗漏导致的口径偏差）
- 彩蛋事件概率为 0（如手改库）时 `random.choices` 全零权重抛 `ValueError` 导致签到整体失败：概率非正事件被过滤，概率与保底触发路径均不再崩溃
- `lottery_tiers` 配置非法（WebUI 手改坏 JSON）时：配置加载阶段校验并回退默认档位 + 告警日志；抽奖运行时解析失败返回明确提示（"抽奖档位配置异常，请联系管理员"）而非通用"抽奖失败"
- `/修改兑换` 对不存在的物品会先执行静默空更新再提示：存在性校验提前到字段解析之前（反馈仍展示修改后全字段信息）
- 指令图 handler 对配置值类型异常（如冷却被 WebUI 写成字符串）会异常冒泡：统一兜底"指令图生成失败"文案
- 指令图缓存清扫（sweep）删除文件后未同步内存索引 `_mem`：现同步移除，避免残留失效引用

### 变更

- `/清空数据`（群清空）除积分/签到字段外，同时重置彩蛋保底计数（`lucky_pity` / `unlucky_pity`）与生日（`birthday` / `birthday_year`），本群成员账户回归初始状态

### 测试

- 新增 7 项用例（守卫累计口径、彩蛋零概率、tiers 非法回退与整数上限、抽奖运行时兜底、备份保留份数、指令图坏配置与 sweep 内存同步、群清空重置彩蛋保底与生日），更新 2 项既有断言（数值边界、群清空语义），全量 **164/164** 通过

## v0.3.1 (2026-08-03)

### 修复

- 修复禁用/重载插件时**定时任务从未被真正移除**导致的重复任务累积：`_remove_cron_jobs` 原先调用不存在的 `job.remove()`（`CronJob` 为数据对象，异常被静默吞掉），备份/生日播报任务在每次启用、重载或 `/设置` 热更新后都会在调度器与 `cron_jobs` 表中新增一组，随时间累积大量每日重复触发任务；现改为经 `cron_manager.delete_job(job_id)` 从调度器、处理器注册与数据库三处移除（旧版 AstrBot 无该 API 时回退原逻辑）
- 初始化时幂等清理历史遗留的本插件非持久化 cron 任务行（`persistent=0`、`job_type=basic` 且名称为 `points_backup` / `birthday_announce`，精确匹配不影响其他插件的定时任务），已累积的残留行自动自愈，无需手工删库

### 测试

- 更新 s10：`_remove_cron_jobs` 改为断言 `cron_manager.delete_job(job_id)` 被调用（旧断言仅验证 mock 的 `remove`，无法覆盖真实路径）；`FakeContext` 新增 `delete_job` / `list_jobs` mock；新增存量清理测试（持久化任务、其他插件任务、active_agent 任务均不受影响）

## v0.3.0 (2026-08-03)

### 新增

- 彩蛋欧皇/非酋触发概率与保底次数改为**配置项**（`easter_lucky_probability` / `easter_lucky_pity_count` / `easter_unlucky_probability` / `easter_unlucky_pity_count`，默认 0.005 / 200，0=关闭保底，`/设置` 可调）；`easter_events` 表 `probability` 列仅保留作同类多事件加权权重，`pity_count` 列不再参与触发判定
- 新增**「我的积分」**指令（无前缀「我的积分 / 积分查询」，严格匹配，仅群聊）：展示群昵称+QQ / 当前积分 / 累计签到 / 连签 / 今日签到 / 本群排名 / 最近 5 条本群流水；未注册用户提示先签到；活跃奖励扫描跳过该关键词消息
- 兑换支持管理员**驳回**：`/核销 [通过|pass|驳回|reject] <记录编号> [备注]`，未写动作默认通过（旧格式兼容）；驳回时事务内退回消耗积分（流水 `redeem_refund`，不计入累计获得）并恢复库存；驳回↔通过可互切（驳回改通过重新扣分、允许余额为负，有限库存不足则操作失败）
- 核销/驳回后**群内 @ 通知兑换者**（通过/驳回文案含订单号、物品、备注/原因），发送失败（如兑换者已退群）向当前会话发警告；状态变更结果不受通知成败影响
- 指令图新增「我的积分」条目，`/核销` usage/desc 同步更新

### 变更

- 移除原「取消核销」（verified ↔ pending 双向切换）：`/核销` 对已通过记录执行为幂等提示（"已是通过状态"），三态决策改为通过/驳回互切
- `redeem_records` 新增驳回审计列 `rejected_at` / `rejected_by`（schema v4，旧库自动迁移；与 `verified_at/by` 互斥写入）
- 管理员核销/驳回确认消息回显备注；`/兑换记录` 列表与详情展示 `❌ 已驳回` 状态

### 修复

- 修复旧系统迁移的高保底计数（最高 90）导致签到**疑似一直触发欧皇**：触发判定改配置驱动后（默认保底 200），遗留计数远低于阈值不再强制触发，无需数据迁移

### 测试

- 新增/更新：彩蛋配置化（s6：默认/关闭保底、概率分支、签到集成）、我的积分（s10：handler 完整信息/最近流水/路由；s12：匹配器；s8：活跃奖励跳过）、兑换驳回（s1：三态互切/退分扣分/库存恢复与守卫/允许负/幂等/无效动作白名单；s10：@通知与警告/权限/跨群；s11：状态机与审计列互斥/库存恢复；s15：v1→v4 迁移链路）

## v0.2.2 (2026-08-02)

### 新增

- 签到反馈新增**当日签到排名 / 连签天数 / 当前积分**三行（事务内取当日签到数计算排名与变动后余额）
- 抽奖反馈新增**消耗积分 / 积分变化 / 当前积分**（`lottery_service.draw` 返回新增 `balance` 字段）
- 兑换成功反馈新增**订单号 / 剩余库存 / 积分余额**与"联系管理员核销"提示（`redeem_service.redeem` 返回新增 `record_no` / `remaining_stock` / `balance` 字段）
- `/设置今日口令` 当日已有口令时追加 **"已覆盖"警告**（显示原口令与积分）
- 指令图 `/修改兑换`、`/兑换记录`、`/流水` usage 同步中英参数；抽奖示例由写死 `whl抽奖` 改为 `{passphrase}抽奖` 占位符，口令变更后指令图示例动态跟随

### 变更

- `/修改兑换` 参数2（字段名）支持中文别名（每字段一个中文，英文原词兼容）：
  `价格`=cost、`库存`=stock、`折扣价`=discount_price、`折扣时间`=discount_end_time、`名称`=name、`描述`=description；非法字段明确提示"不支持字段"而非静默按文本写入
- `/修改兑换` 反馈改为显示**修改后该商品全部信息**（名称/描述/价格/库存/折扣价+截止时间/状态）
- `/兑换记录` 与 `/流水` 目标参数支持中文别名：`全部`=all、`未核销`=pending（英文原词兼容，`/流水` 参数错误文案同步）
- `/查生日` 反馈改用**群昵称**（card → nickname → QQ 回退，实现同积分榜）
- 新增 `utils.security.clean_display_name`（控制字符剥离），统一应用于运势、排行、查生日、活跃奖励四处群昵称/发送者昵称拼装点，修复排行与活跃奖励**原有的控制字符注入缺口**（`\n` 等可构造多行伪造消息）

### 测试

- 更新：签到/抽奖反馈消息断言（s1/s11）、兑换成功反馈断言（s1/s11：订单号/剩余库存/积分余额/核销提示）、`/修改兑换` 全字段反馈（s9）、`/设置今日口令` 覆盖警告（s9）、口令变更后保留字动态跟随（s9：`喵喵签到`/`签到喵喵`/`喵喵抽奖`/`抽奖喵喵` 被拒、`whl抽奖` 放行）、`/兑换记录 全部|未核销` 与 `/流水 全部` 路由（s10）、`/查生日` 群昵称（s7）、排行恶意昵称剥离（s18）、活跃奖励昵称剥离（s8）、fortune 剥离重构一致性（s12）

## v0.2.1 (2026-08-02)

### 修复

- 无前缀触发改为**严格匹配**：签到/排行要求消息完全等于触发词，抽奖要求等于「关键词 / 口令+关键词 / 关键词+口令」；带附加文本的消息（如"我要签到"、"whl 今天抽奖"）不再触发，普通聊天含关键词字样不再被静默吞掉（LLM 正常回复）。`stop_event` 仅在产生实际输出后调用，消除对管理命令的误拦截（如 `/设置今日口令 抽奖 10` 不再被抽奖正则抢先吞掉）
- 口令保留字校验：`/设置今日口令` 关键词与签到/抽奖/排行触发词相同、或构成「口令±触发词」组合（如 `whl抽奖`）时拒绝设置，杜绝口令死锁（该形态消息会被触发词拦截、口令永远领不到）
- 每日口令**全局限领 1 次**：同用户当天在任意群已领取过口令则拒绝（与口令内容、群无关），杜绝跨群刷分；新增 `idx_dk_claim_qq_date` 索引
- 每日抽奖限次改为**按 QQ 全局统计**：各群不能分开刷满限额；新增 `idx_lottery_qq_date` 索引
- 签到流水补 `ref_id` 关联签到日志 id；签到热路径去掉重复的账户建行/读取
- `/核销` 备注经 `sanitize_text` 清洗（截断 200），防超长/控制字符撑爆回显
- schema 迁移清理跨群同日重复签到行时记录删除数量并告警（审计提示）
- `terminate()` 等待缓存清扫任务真正结束，避免任务泄漏

### 变更

- 移除 `_PLUGIN_COMMANDS` 命令白名单（严格匹配已完全覆盖其防护职责）

### 测试

- 更新：活跃奖励跳过判定（严格形态消息）、main 路由（wake 跳过/触发判定）、s17 目录断言（白名单移除）
- 新增：严格匹配边界（"我想抽奖"/"签到！"不触发）、口令全局限领、抽奖全局限次、口令保留字三形态拒绝

## v0.2.0 (2026-08-02)

### 重大变更

- **积分一号跨群共享**：新增 `accounts` 表（按 QQ 全局唯一账户），积分/累计获得/签到状态（连签/累计天数/彩蛋保底）全部迁入；`users` 表瘦身为仅存群级数据（成员关系 + 负分头衔）。schema v2 → v3 自动迁移（多群余额取 MAX 合并、删除 users 冗余列、sign_in_log 增加 `(qq, sign_date)` 全局唯一索引）
- **每天全局限签 1 次**：同用户跨群同日签到被拒，连签计数跨群延续
- **连签奖励修复**：从第 2 天起额外加分（第 N 天 = `(N-1) × per_day`），第 1 天不再白得奖励
- **排行榜显示群昵称**：展示时实时调用 `get_group_member_info` 取群名片（card → nickname），失败回退 QQ 号；签到统计的首签/连签王同步显示昵称
- **`/清空数据`（群）语义变更**：清零本群成员的共享积分与群级记录（积分全局共享，其他群余额同步归零），成员关系保留；确认文案增加全局影响预警
- **负分头衔跨群联动**：余额回正后清除用户在**所有群**的负分头衔并恢复原名片

### 重构

- 改分唯一入口：`PointService.change_balance`（事务内原语，含余额守卫/累计获得控制/自动建行），签到/抽奖/兑换/口令/管理加减分全部收敛，消除 5 处重复 SQL
- 平台成员信息获取收敛到 `utils/group_info.fetch_member_info`（负分头衔原名片与排行昵称共用）
- 删除死字段：`users.max_consecutive_days`、`users.birthday_bonus_claimed`（业务零读取）、`users.platform`（收敛到 accounts）

### 测试

- 测试套件由 135 项扩充至 142 项：新增跨群共享套件（s18）——跨群余额一致/全局限签/连签跨群延续/负分跨群联动/排行昵称（card/昵称/异常三级回退）/群清空新语义/管理扣分负数建行/并发跨群签到仅 1 次成功；更新 s1/s2/s6/s7/s9/s10/s11/s14/s15/s16 至 accounts 新模型

## v0.1.4 (2026-08-01)

### 修复

- `/扣分` 目标用户从未注册（无 users 行）时不再"假成功"：`point_service.subtract` 自动建行后如实扣成负数，流水 `balance_after` 与实际余额一致，扣负自动补发负分头衔
- `/兑换记录` 纯数字参数视为页码（与 `/流水` 一致）：`/兑换记录 2` 正确显示第 2 页，`all` / `pending` / 记录编号语义不变
- 配置热生效：`/设置 signin_refresh_time` 立即更新业务日分界；`/设置 backup_time` / `birthday_announce_time` / `backup_enabled` 即时重建定时任务（先移除旧任务再注册，不再残留孤儿任务）；`birthday_announce_time` 非法值回退默认而非中断注册
- `on_lottery` / `on_ranking` 处理结束后调用 `stop_event()`，与签到行为一致，避免抽奖/排行后 LLM 二次回复
- 每日口令清除（`clear_daily_keyword`）两条 DELETE 合并为单事务，中途失败整体回滚，不再出现"领取记录已删、口令残留"导致的重复领取
- 清空验证码令牌（`_pending_clears`）增加过期清理，长期未确认的条目不再驻留内存
- 非酋彩蛋导致签到总获得为负时，回复文案正确显示 `-N 积分` 而非 `+-N`
- 迁移时 `PRAGMA table_info` 游标补 `close()`，避免长连接资源泄漏
- 全局清空后的彩蛋事件重播种改走持锁事务，消除与其他写操作的并发竞争
- 运势文案对昵称剥离控制字符（`\r\n` 等），昵称无法再构造多行伪造消息
- ruff 存量问题清理：`Optional` 注解现代化、导入排序、未用变量/导入（`db/dao.py`、`services/sign_in_service.py`、`handlers/admin.py`、`utils/fortune.py`）

### 测试

- 测试套件由 133 项扩充至 135 项：新增清空令牌过期清理（s9）；扩展未注册用户扣分自动建行（s11）、`/兑换记录` 页码路由与 cron 重建 / stop_event（s10）、`/设置` 时间类配置热生效（s9）、非酋彩蛋消息格式（s6）、运势昵称控制字符剥离（s12）

## v0.1.3 (2026-08-01)

### 新增

- `/积分系统帮助`（别名：指令图 / 命令图 / 帮助图）：生成插件全部指令总览图。渲染三级降级（HTML 海报 → Markdown 文转图 → 纯文本），图按「指令目录 + 配置关键词/口令 + 插件版本」签名在本地长时缓存，配置或版本变化后下次触发自动强制重新渲染；用户/群双限流防刷
- 指令图动态反映运行时配置（签到/抽奖关键词、抽奖口令），HTML 数据统一转义，不渲染任何敏感配置（备份路径/白名单/管理名单），全程不访问数据库
- 指令图渲染系统配置化（WebUI 与 `/设置` 双向同步、热生效）：`cmd_map_user_cooldown`（同用户冷却秒数，0=不限）、`cmd_map_group_cooldown`（同群冷却秒数，0=不限）、`cmd_map_cache_ttl_hours`（缓存有效期小时，0=禁用缓存每次强制重新渲染）；缓存 TTL 热失效（无需等待清扫），清扫周期按 TTL 自动派生

### 测试

- 测试套件由 118 项扩充至 133 项，新增指令图套件（s17）：目录完整性（27 指令 + 3 别名反查 main.py 注册）、动态关键词注入、Markdown/海报数据转义、签名稳定性与配置/版本换键、缓存存取/失效/超限透传/过期清扫、TTL 热失效与 0=禁用缓存、三级降级链路、HTTP 异常返回兜底、限流与冷却配置读取、新配置项 `/设置` 校验、私聊场景

## v0.1.2 (2026-08-01)

### 变更

- `/扣分`（`point_service.subtract`）允许将余额扣成负数：移除"余额不足"拒绝，超额扣分照常入账（流水如实记录负余额），扣负后自动补发"群女仆X号"头衔、回正后自动恢复。负余额来源不再仅限于签到非酋彩蛋

### 修复

- 修复 `lottery_tiers` 权重为 `NaN`/`Infinity`（Python `json.loads` 接受此类字面量，可绕过 `weight <= 0` 校验）时抽奖 `random.uniform(0, nan)` 直接崩溃的问题：权重校验增加 `math.isfinite`，非有限值一律拒绝

### 测试

- 测试套件由 35 项扩充至 118 项（零依赖，`python -m tests.run_all` 运行，全部在临时库上执行、不触碰生产数据），新增 11 个套件：
  - 彩蛋/日期奖励：概率/权重/保底 pity/签到正负彩蛋集成（负彩蛋→负余额→负分头衔、`total_earned` 排除）、日期奖励单日/跨年区间/概率/停用排除/签到联动
  - 生日：设置/查询/非法输入、播报服务三态、签到生日奖励与年度去重、定时播报幂等
  - 活跃奖励：命令/签到/抽奖消息跳过、最短长度/用户冷却/全局冷却/概率/负分跳过、加分入账与发送、与每日口令同消息双触发
  - 管理指令成功路径：加减分（含扣成负数）、商品增删改全字段、口令设置/清除、配置（交叉校验/WebUI 落库）、折扣、管理员增删、日期奖励增删查、全局清空全流程（表清空/彩蛋重种/备份/负分名片恢复）
  - 用户侧 handler 与 main 路由：非群拒绝、空榜/全局榜/统计全分支、兑换列表/记录详情/核销权限、`/流水` 权限矩阵、唤醒命令跳过、兑换参数路由、cron 注册
  - 服务边界与积分不变量：抽奖空档位/不限次数/中奖入账、兑换 ∞库存/余额恰等/折扣过期、`total_earned` 排除表、负分头衔与群名片（FakeBot 断言调用）、DAO 全方法（分页/过滤/白名单/幂等）
  - 工具/配置：`parse_keyword_list`、业务日分界解析与回退、today/period 一致性、跨年区间、兑换编号递增、安全解析各形态、运势文案
  - 限流器：用户/群冷却语义、cooldown≤0 直通、剪枝、clear；备份恢复：唯一序号、VACUUM 快照重开一致性、多目录部分失败隔离
  - 迁移：schema v1→v2 加列、重复迁移幂等、legacy 配置迁移（类型转换/非法值跳过/清空旧表）
  - 压力/浸泡/随机化：300 混合并发全局余额对账、3 个业务日浸泡（每日限额重置/口令缓存淘汰/限流剪枝）、200 步随机操作序列 fuzz 不变量、30 组随机配置一致性
- 扩展并发测试 3 项（并发兑换编号唯一、并发添加管理恰 1 行、并发播报标记幂等）与安全测试 3 项（字段白名单注入、NaN/Infinity 权重注入、日期奖励概率/日期边界）
- 测试设施：`astrbot.api` 全量桩（filter/star/platform/MessageChain 链式）、FakeEvent 增强（群主判定/发送记录）、FakeBot/FakeContext、确定性随机助手（`patch_random`）、业务日分界保存-恢复，杜绝跨测试全局状态污染

### 优化

- `point_service` 代码风格与 ruff 对齐（导入排序、`Optional` 注解现代化），`ruff format` / `ruff check` 全部通过

## v0.1.1 (2026-08-01)

### 变更

- 抽奖奖励由倍率制改为固定积分区间：`lottery_tiers` 每档的 `multiplier` 字段替换为 `points_min` / `points_max`，命中档位后在闭区间内随机获得积分；`lottery_cost` 默认值由 100 调整为 20，默认五档改为 特等奖 100 / 一等奖 31-45 / 二等奖 21-30 / 三等奖 11-20 / 四等奖 1-10（权重 2/18/37.5/27.5/15）。旧 `multiplier` 配置不再通过校验，需重新 `/设置 lottery_tiers`

## v0.1.0 (2026-07-31)

### 新增

- 活跃奖励积分由固定值改为随机区间：新增配置项 `active_reward_points_min` / `active_reward_points_max`（默认 1 / 5，闭区间），替换原 `active_reward_points`；`/设置` 增加两值交叉校验（min ≤ max），WebUI 填反时代码端自动钳制
- 新增数据清空指令（验证码二次确认）：`/清空数据` 清空本群积分数据（群管理员）；`/清空全部数据` 清空全部数据（仅 AstrBot 全局管理员）；`/确认清空 <验证码>` 执行（5 分钟有效、单次有效）。清空前自动备份至 `backup_before_clear/` 并尽力恢复负分头衔原群名片

### 变更

- 配置项 `active_reward_points` 废弃（残留值由 AstrBot 配置完整性检查自动清理）

### 修复

- 修复 `/设置` 指令热更新不生效的问题：配置缓存改为就地更新，签到/抽奖/备份相关配置修改后立即可用，无需重载
- 修复 `/兑换记录 all|pending` 与单条详情跨群越权查看的问题：群管理员仅限本群记录，AstrBot 全局管理员可跨群查看
- 修复事务回调内调用 db/dao 方法可能永久死锁的问题：所有数据库入口增加锁占用断言，误用时抛出清晰错误
- 修复备份目标文件已存在时 `VACUUM INTO` 失败的问题：同日多次备份自动追加序号（`_2`、`_3`…）
- 修复死锁守卫在并发下误伤正常等待锁的任务的问题：改为按持锁任务（`_lock_owner`）追踪，仅拦截真正的事务回调内重入
- 修复独立读连接在 WAL 下快照滞留、写入后立即读取偶发读不到刚提交数据的问题：回退为单连接读写串行化（正确性优先），3 万次并发读后写压力测试零遗漏
- 修复 `/设置今日口令`（INSERT OR REPLACE）与 `/清除今日口令` 在已有领取记录时触发外键约束失败的问题：改用 UPSERT、清口令前先删领取记录

### 优化

- 配置默认值改为单一来源（`_conf_schema.json` 生成），消除与代码内默认值的漂移风险
- `total_earned` 排除清单改为显式常量 `_EARNED_EXCLUDED_REASONS`，语义清晰
- 备份/生日播报 cron 移除固定 `Asia/Shanghai` 时区，与每日刷新时刻统一按宿主机本地时区
- 数据库游标全部显式关闭（`async with` / `try-finally`），避免长期运行资源堆积
- `HH:MM` 类配置（刷新/备份/播报时间）接受非补零输入（如 `4:00`）并规范化为 `04:00` 存储，WebUI 与指令行为一致
- `schema_version` 读取增加防御：值损坏时自动重写为当前版本，不再导致插件加载失败
- `/修改兑换 discount_price` 增加"折扣价须低于原价"校验，与 `/设置折扣` 行为对齐
- 连签王并列时按最早签到时间决胜；运势抬头改为显示群昵称（缺省回退 QQ 号）
- 兑换记录编号前缀改用业务日日期，与每日刷新边界一致
- 抽奖/每日口令的负分拦截移入事务内（保留事务外快速检查），消除竞态窗口
- `/兑换记录` 个人列表限定当前群，不再展示其他群的记录

### 测试

- 新增 `tests/` 测试套件（零依赖，`python -m tests.run_all` 运行）：功能正确性 11 项、并发/竞态 9 项、安全测试 7 项（SQL 注入 fuzz、路径穿越、权限矩阵、令牌爆破、超长输入）、稳定性/故障注入 8 项，以及性能压测基准（消息扫描/签到/排行/流水/抽奖/内存），全部在临时库上运行，不触碰生产数据
- 测试发现并修复 3 个隐藏问题：死锁守卫并发误伤、读连接 WAL 快照滞留（回退单连接）、口令 UPSERT 外键冲突

### 文档

- README 功能列表按「积分获取 / 积分消费 / 排行与统计 / 管理 / 系统机制」分组重构，描述精简

## v0.0.4 (2026-07-31)

### 新增

- 配置项全面接入 AstrBot WebUI（新增 `_conf_schema.json`），可在插件设置页查看和修改，与 `/设置` 指令双向同步；旧版数据库中的配置在首次加载时自动迁移到托管配置文件（`data/config/`）并清理旧表
- 新增配置项 `signin_refresh_time`（默认 `04:00`）：每日刷新时刻，签到/连签/每日首签/每日口令/抽奖次数等每日逻辑按该时刻切换新一天（一天区间为该时刻至次日同一时刻）
- 新增配置项 `backup_time`（默认 `04:00`）：每日自动备份时刻

### 变更

- 每日备份时间由固定 03:00 改为配置项 `backup_time`，默认 04:00
- 每日刷新点由自然日 00:00（宿主本地时区）改为配置项 `signin_refresh_time`，默认 04:00

### 优化

- 抽奖每日次数统计改为按刷新区间起点（`created_at >= 区间起点`）统计，与刷新时刻语义一致，避免 00:00–04:00 区间漏计

## v0.0.3 (2026-07-31)

### 修复

- 修复无限库存（`stock=-1`）商品被反复扣成负数、最终报"库存不足"的问题（仅在非无限库存时递减）
- 修复 `/签到统计` 被无前缀签到正则抢占的问题（命令消息不再误触发签到）
- 修复 `/修改兑换` 可将价格/折扣改为负数导致兑换反加积分的问题，按字段增加边界校验（cost/discount_price ≥ 1、stock ≥ -1、折扣截止时间须为 `YYYY-MM-DD HH:MM`）
- 修复 `/设置 backup_dirs` 后重启路径被拆分损坏的问题（列表类配置统一 JSON 存储）
- 修复负分头衔在积分回正后未清除的问题，并实际接入群名片：积分为负时调用 `set_group_card` 设置"群女仆X号"、回正后恢复原群名片（新增 `negative_title_prev_card` 字段，schema v2 自动迁移）
- 修复彩蛋保底计数器在事务外更新、签到失败时保底误增的问题（保底计数并入签到事务）
- 修复 `/流水` 页码与 @用户 解析冲突：纯数字视为页码，支持 `@QQ` / `@昵称(QQ)` / CQ at 形式
- 修复 `/设置 lottery_tiers` 缺少档位 `label` 校验导致抽奖 KeyError 的问题
- 修复生日播报"先标记后发送"导致发送失败当日无法重试的问题（发送成功后才标记）

### 变更

- 积分流水新增 `admin_qq` 审计字段（schema v2），加分/扣分记录操作人
- 非酋负事件不再扣减累计获得（`total_earned`）
- `/加分`、`/添加管理`、`/删除管理`、`/查生日` 支持 @提及 解析
- 插件版本号统一为 `v0.0.3`（metadata 与 @register 对齐）

### 优化

- 活跃奖励冷却记录与每日口令缓存增加自动清理，避免长期运行内存增长
- `/设置今日口令` 空关键词拒绝写入；`/设置折扣` 截止时间格式校验
- 积分变动后统一对账负分头衔状态（签到/口令/抽奖/兑换/活跃奖励/管理员操作）

## v0.0.2 (2026-07-31)

### 新增

- 新增配置项 `lottery_daily_limit`：每日抽奖次数上限，默认 10
- 新增配置项 `backup_dirs`：备份目标目录列表（原备份目录指令移除），相对路径基于插件数据目录解析，适配云服务器部署
- 新增管理员指令：
  - `/添加管理 <QQ号>` / `/删除管理 <QQ号>` — 群内积分系统管理员管理（仅限群主/全局管理员）
  - `/添加日期奖励 <MM-DD|MM-DD~MM-DD> <关键词> <积分> [概率]` / `/删除日期奖励 <ID>` / `/查看日期奖励` — 日期奖励的完整增删查
- 新增配置校验体系（`config/defaults.py`）：类型转换、范围校验（随机签到上下限互斥、活跃奖励概率 0~1、抽奖档位权重/倍率合法性、口令非空、生日播报时间 HH:MM 格式）
- 关键词配置兼容 JSON 数组与逗号分隔文本，空关键词拒绝写入

### 变更

- 备份目录由 `/设置备份目录`、`/清除备份目录` 指令改为配置项 `backup_dirs`，通过 `/设置 backup_dirs <列表>` 热更新
- 备份服务改为读取配置缓存，移除 `backup_configs` 数据表及其 DAO 方法

### 优化

- 每日口令服务增加缓存失效机制（`invalidate`），设置/清除口令后立即热生效
- 积分变动改为事务化写入，`ensure_negative_title` 负分头衔逻辑独立校验
- 积分流水与签到统计功能完善（`/流水` 支持分页与指定用户、`/签到统计` 展示今日签到数据）
- 抽奖服务增强：每日限额与口令校验逻辑整合到服务层
- 数据库连接与 DAO 层重构，减少冗余代码

### 修复

- 修复管理员增减积分、兑换折扣等操作的参数校验与错误提示
- 修复负分用户相关判定与头衔撤销时机

## v0.0.1 (2026-07-30)

### 初始版本

- 每日签到：固定/随机积分、首次签到奖励、每日首签奖励、连签奖励（有上限）、每 7 天奖励，签到回复尾附每日运势
- 彩蛋事件：签到概率触发欧皇/非酋事件，±大量积分，独立保底计数器
- 活跃奖励：群消息概率获得积分，用户冷却 + 全局冷却
- 每日口令、日期口令（支持跨年）
- 无前缀关键词触发（签到/抽奖/排行）
- 群内排行（Top 10，不足 3 人回退全局）
- 个人抽奖：口令验证，五档权重概率
- 兑换玩法：库存管理、兑换记录、核销状态双向切换、限时折扣
- 管理员指令：独立管理员名单、增减积分、管理兑换/口令/配置
- 生日系统：记录生日、生日签到奖励、定时播报
- 负分联动：限制抽奖/兑换/活跃奖励，自动分配"群女仆X号"头衔
- 自动备份：多目标目录，每日 03:00 定时备份，备份前 WAL checkpoint
- 积分流水与签到统计
